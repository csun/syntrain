This directory is populated after running `convert_nyu_v2.py`. Extra exploratory data is stored in `supplemental/`. This includes stuff like `classes.csv` - the counts of class appearances in images.
